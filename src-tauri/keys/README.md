# Updater keys (local)

- `updater.key` — **private**, gitignored. Do not commit or share.
- `updater.key.pub` — public; content is also in `tauri.conf.json`.

Password used when generating (keep with private key backups): `.`

See [docs/release-updates.md](../../docs/release-updates.md).
